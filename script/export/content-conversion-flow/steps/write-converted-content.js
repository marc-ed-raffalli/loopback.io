'use strict';

module.exports = function (structure, pageDetails, markdownStr, callback) {

  // TODO write the file to the Jekyll template

  console.log('Processed page', pageDetails.title);

  callback(null);

};
